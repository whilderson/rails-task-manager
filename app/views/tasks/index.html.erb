<h1>All the tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> - <%= task.details %>
    - status: <%= task.completed ? "compeleted" : "pending" %></li>
  <% end %>
</ul>
<p><%= link_to "Create new task", tasks_new_path %></p>
